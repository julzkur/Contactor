import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#555",
  },
  title: {
    fontSize: 25,
    fontWeight: "bold",
    color: "#249",
  },
});

export default styles;
